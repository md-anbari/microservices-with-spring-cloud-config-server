spring:
  application:
    name: notification-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      username: configuser
      password: configpassword
      fail-fast: false 