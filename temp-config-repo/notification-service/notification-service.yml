# Common configuration for notification service

# Management endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh,env
  endpoint:
    health:
      show-details: always

# Application specific settings
app:
  notification-service:
    simulate-sending: true
    default-sender: system@example.com
    throttle-limit: 100 