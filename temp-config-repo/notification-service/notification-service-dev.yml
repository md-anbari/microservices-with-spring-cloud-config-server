# Development-specific configuration

# Logging configuration
logging:
  level:
    com.example.notificationservice: DEBUG
    org.springframework.web: INFO

# Development settings
app:
  notification-service:
    simulate-sending: true
    throttle-limit: 5
    retention-days: 30 